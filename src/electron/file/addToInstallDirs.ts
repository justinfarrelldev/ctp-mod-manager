export const addToInstallDirs = (dir: string) => {
  console.log('would check if the file for installs is there');
  console.log('if it is not, it would create one with "[]" inside');
  console.log('after that, it would pull the current one and then push the dir to it');
  console.log('dir: ', dir)
};
